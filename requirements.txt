# Phase 2B local text/tool runtime. The official Ollama client is always constructed
# with an explicit loopback endpoint and bounded httpx timeouts.
httpx>=0.27,<1
ollama>=0.6.2,<0.7

# Phase 2C1 microphone capture. RawInputStream avoids a NumPy runtime contract;
# the Windows wheel includes the PortAudio pieces needed by sounddevice.
sounddevice>=0.5.6,<0.6

# Phase 2C2 local CPU speech synthesis. Models/voices are installed explicitly
# by scripts/setup_tts_windows.ps1 and never downloaded during normal startup.
kokoro-onnx==0.6.1
piper-tts==1.7.0

# Phase 2C3 local wake word plus the ONNX-only Silero VAD wrapper. The setup
# script installs pinned, hash-verified auxiliary models; no PyTorch is used.
openwakeword==0.6.0

# Development tests remain isolated from Ollama, network, GPU, and hardware.
pytest>=8.3,<9
